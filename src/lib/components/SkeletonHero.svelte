<script lang="ts">
	/**
	 * A placeholder in the shape of the Safe-to-spend hero: the headline figure
	 * and the line beneath it, so the card is real paper while the forecast is
	 * still being computed. Shown only while the streamed forecast promise is in
	 * flight — the card's label and frame belong to the shell around it.
	 */
</script>

<div aria-hidden="true">
	<div class="skeleton mt-2.5 h-8 w-[172px]" style="--i: 0; --j: 0"></div>
	<div class="skeleton mt-3.5 h-3.5 w-[228px]" style="--i: 1; --j: 1.5"></div>
</div>
